# LangChain və LangGraph — Example Code (3)

Ev tapşırığı və lokal sayt: `03-tools/`

Saytı işə salmaq:

```bash
cd nvidia-langchain-langgraph/03-tools
python3 -m uvicorn web.app:app --host 0.0.0.0 --port 8080
```

http://localhost:8080
